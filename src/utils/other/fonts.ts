import { Inter, Playfair, JetBrains_Mono } from 'next/font/google'
 
export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
})
 
export const playFair = Playfair({
  subsets: ['latin'],
  display: 'swap',
})

export const jetBrains_Mono = JetBrains_Mono({
  subsets: ['latin'],
  display: 'swap',
})